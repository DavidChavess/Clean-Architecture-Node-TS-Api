import { accountModelSchema, errorModelSchema, loginParamsSchema, surveyResultModelSchema, surveyResultParamsSchema } from './schemas/'

export default {
  accountModelSchema,
  loginParamsSchema,
  errorModelSchema,
  surveyResultParamsSchema,
  surveyResultModelSchema
}
